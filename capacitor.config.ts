import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ua.pp.romansmolnyk.outliner',
  appName: 'RS Outliner',
  webDir: 'dist'
};

export default config;
